# =================================
# Cerebelum Core - Production Environment Variables
# =================================

# Database Configuration (for external PostgreSQL)
# Using host network mode, PostgreSQL on same server is accessible via localhost
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=cerebelum
DATABASE_PASSWORD=change_this_secure_password
DATABASE_NAME=cerebelum_prod

# Application Secrets
# Generate with: mix phx.gen.secret
SECRET_KEY_BASE=change_this_to_a_64_character_secret_generated_by_mix_phx_gen_secret

# Host Configuration
PHX_HOST=your-domain.com
PORT=4001

# gRPC Configuration
ENABLE_GRPC_SERVER=true
GRPC_PORT=9090

# Erlang/OTP Release Configuration
RELEASE_NODE=cerebelum@127.0.0.1
RELEASE_COOKIE=change_this_to_a_secure_cookie

# Database Pool Size
POOL_SIZE=10
